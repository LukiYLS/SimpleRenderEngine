#include "ObjectControl.h"

namespace Utils {

	void ObjectControl::receive(Event::ptr event)
	{
		//only receive event has been registerred,so where register event,i guess when i create objectcontrol,
	}
}